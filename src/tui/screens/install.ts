// Install flow modal — stub: real flow wires scope + target checkboxes + env inputs.

import type { RegistryItem } from '../../registry/types.js'

export async function launchInstallFlow(item: RegistryItem): Promise<void> {
  throw new Error(`install flow not yet implemented (item=${item.id})`)
}
